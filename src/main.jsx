import React from 'react'
import ReactDOM from 'react-dom/client'
import SmartTravelPlanner from './SmartTravelPlanner'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SmartTravelPlanner />
  </React.StrictMode>
)
